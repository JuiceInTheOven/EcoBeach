{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionString": {
    "SwimSafeDB": "server=((server name));database=SwimSafe;User ID=(( server user id));password=(( server password));",
    "LocalDB": "Server=localhost;Database=master;Trusted_Connection=True;"
  },
  "AllowedHosts": "*"
}
